#include <stdio.h>
#include <string.h>

/**
 * main - main function of the project
 *
 * Return: returns value 0 everytime
 */
int main(void)
{
	char display[50];
	strcpy(display, "\"Programming is like building a multilingual puzzle");
	puts(display);
	return (0);
}
